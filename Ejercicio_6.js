var numero = prompt("Ingrese el número: ");
function absoluto(numero)
{
    let valor;
    if(numero>0)
    {
        valor=parseInt(numero);
    }
    else
    {
        valor=parseInt(numero);
    }
    return (valor);
}
alert(absoluto(numero));